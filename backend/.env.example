# Backend env vars
# Copy to .env (do not commit .env)

# Server
PORT=5001
NODE_ENV=development

# Database (use local for dev, Atlas for prod)
# Local example:
# MONGODB_URI=mongodb://localhost:27017/wealthify
# Atlas example:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/wealthify?retryWrites=true&w=majority
MONGODB_URI=

# Auth
JWT_SECRET=change_me_to_a_long_random_string

# CORS
# In production, set to your Vercel URL (e.g. https://your-frontend.vercel.app)
CLIENT_ORIGIN=http://localhost:3000
