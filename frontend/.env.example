# Frontend env vars (Create React App)
# Copy to .env (do not commit .env)

# Local dev default is localhost; set this in Vercel to the hosted backend
# Example: https://wealthify-backend.onrender.com/api
REACT_APP_API_URL=http://localhost:5001/api
